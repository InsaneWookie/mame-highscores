How to manually create a new password
From node interpreter (assuming you are in a directory where bcrypt package is installed)

var bcrypt = require('bcrypt');
bcrypt.hashSync('pass', bcrypt.genSaltSync(10));


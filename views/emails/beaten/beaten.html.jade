doctype html
html(lang='en')
  head

  body
    div Your high score of <b>#{beaten.score}</b> on <b>#{game.full_name}</b> has just bean beaten by <b>#{beatenBy.user.username}</b>
    | with a score of <b>#{beatenBy.score}</b>
    div View here:
      a(href="http://#{siteUrl}/#games/#{game.id}") #{siteUrl}/#games/#{game.id}